# Ultra MCP SmartScreen Server dependencies

mcp[cli]>=1.28.1,<2      # FastMCP Streamable HTTP runtime + CLI
fastapi>=0.95.0          # Web server framework (FastAPI)
uvicorn[standard]>=0.30.0   # Supports --no-access-log, --log-level, etc.
httpx>=0.27.0,<1         # ASGI client dependency used by MCP/FastAPI tests
python-dotenv>=1.0.0     # Load .env configuration
